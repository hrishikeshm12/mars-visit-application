import React from 'react';

const FormNavigation = ({ stage }) => {
  return (
    <div className="form-navigation">
      <span>Stage {stage} of 3</span>
      {/* Add progress bar or other navigation indicators */}
    </div>
  );
};

export default FormNavigation;
